/*
 * OverlayHOC Messages
 *
 * This contains all the text for OverlayHoc
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.component.OverlayHoc';

export default defineMessages({
  buttonJoin: {
    id: `${scope}.buttonJoin`,
    defaultMessage: 'Join',
  },
});
